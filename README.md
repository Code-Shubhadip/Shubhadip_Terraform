This is my First terraform project.
I have created an EC2 instance configuring the n/w settings with default security group & VPC.
Defined the inbound & Outbound rules.
Also after ec2 instance is created automated the connection through ssh and downloading an nginx application on the instance.
